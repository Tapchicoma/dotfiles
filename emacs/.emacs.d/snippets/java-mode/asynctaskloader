# -*- mode: snippet -*-
#name : asynctaskloader
#key : asynctaskloader
# expand-env : ((yas/indent-line 'fixed) (yas/wrap-aroud-region))
# --
import android.content.Context;
import android.content.AsyncTaskLoader;

/**
 * $1.
 */
public class ${2:`(file-name-sans-extension (buffer-name))`} extends AsyncTaskLoader<$3> {
    //
    // Interfaces
    //

    //
    // Fields
    //

    //
    // Constructors
    //

    /**
     * Main.
     * @param context app context
     */
    public $2(final Context context) {
        super(context);
    }

    //
    // Nested classes
    //

    //
    // Methods
    //

    /**
     * {@inheritDoc}
     */
    @Override
    public final $3 loadInBackground() {
        $0
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public final void deliverResult(final $3 data) {
        if (isStarted()) {
            super.deliverResult(data);
        }
    }

    /**
     * {@inheritDoc}
     */
    @Override
    protected final void onStartLoading() {
        forceLoad();
    }

    /**
     * {@inheritDoc}
     */
    @Override
    protected final void onStopLoading() {
        cancelLoad();
    }

    /**
     * {@inheritDoc}
     */
    @Override
    protected void onReset() {
        super.onReset();
    }
}